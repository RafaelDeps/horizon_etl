prefect==3.6.23
# prefect 3.6.23 subclasses fastapi's APIRouter in a way that is broken by the
# router redesign in fastapi>=0.136. fastapi 0.141.x/starlette 1.x crash the
# temporary Prefect API server with
# "AttributeError: 'PrefectRouter' object has no attribute 'routes'".
# Pin fastapi to the generation that shipped alongside prefect 3.6.23.
fastapi==0.115.12
loguru>=0.7.0
pydantic>=2.0.0
supabase>=2.0.0
pandas>=2.0.0
pyarrow>=15.0.0
networkx>=3.4.0
requests>=2.30.0
beautifulsoup4>=4.12.0
openpyxl>=3.1.0
thefuzz>=0.22.0
rapidfuzz>=3.0.0
python-Levenshtein>=0.25.0
python-dotenv>=1.0.0
# agent_sigpesq: fixado no SHA da branch feat/project-pdf-download-and-mistral-extraction,
# que acrescenta o download dos PDFs por projeto e a extração PDF->JSON via Mistral
# (produz os data/exports/project_sigpesq_files_json/PJ_*.json que o enrich_projects consome).
# SHA em vez de nome de branch porque branch não é build reprodutível — o commit muda
# debaixo do projeto. Trocar por uma tag (ex.: v0.4.0) assim que a branch for liberada.
# O extra [extract] traz mistralai e pypdf; a extração exige MISTRAL_KEY no .env.
agent_sigpesq[extract] @ git+https://github.com/ifesserra-lab/sigpesq_agent.git@8f41f7a9504e3f281a342d3e449ace03574b5b13
git+https://github.com/ifesserra-lab/dgp.cnqp_lib.git@v0.5.0
scriptLattes @ git+https://github.com/ifesserra-lab/scriptLattes.git@v0.8.0
selenium>=4.0.0
research-domain @ git+https://github.com/The-Band-Solution/ResearchDomain.git@v0.14.4
